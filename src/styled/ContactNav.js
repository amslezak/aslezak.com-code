import styled from "react-emotion";
import {
  space,
  width,
  fontSize,
  letterSpacing,
  lineHeight,
  textAlign,
  alignSelf,
  borderRadius
} from "styled-system";

const Contact = styled.div`
  ${space};
  ${width};
  ${fontSize};
  ${letterSpacing};
  ${lineHeight};
  color: #43a6b1;
  cursor: pointer;
`;

export default Contact;
